#include "main.h"
/**
* print_alphabet_x10 - prints 10 times the alphabet, in lowercase
* Return: always 0
*/
void print_alphabet_x10(void)
{
int x;
char m;
for (x = 0 ; x <= 9 ; x++)
{
for (m = 'a' ; m <= 'z' ; m++)
{
_putchar(x);
}
_putchar('\n');
}
}
