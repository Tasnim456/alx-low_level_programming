#include "main.h"
/**
* _abs - a function that computes the absolute value of an integer.
* @c : is the number we need to get its absloute
*Return: the absloute value
*/
int _abs(int c)
{
if (c >= 0)
{
return (c);
}
else
{
return (-c);
}
}
